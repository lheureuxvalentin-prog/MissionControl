services:
  mission-control:
    build: .
    network_mode: host
    volumes:
      - ./data.json:/app/data.json
      - mc_state:/app/state
    env_file: .env
    restart: unless-stopped

volumes:
  mc_state:
